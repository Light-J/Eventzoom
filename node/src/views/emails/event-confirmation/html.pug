doctype html
body
    h1 Event confirmation for #{event.title}
    p Your booking is confirmed for this event
    a(href=event.calendarLink)
        h3 Click here to add to your calendar (Open the event file to import to your calendar

    h3 Event details
    p Location: #{event.specificLocation}
    p Organiser: #{event.organiser.email}
    p Date: #{event.date.toLocaleString()}

